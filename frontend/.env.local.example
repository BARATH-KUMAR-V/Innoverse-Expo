# Copy this file to ".env.local" and fill in the real value.
# Local dev: the backend running on your machine.
# Production (Vercel): your deployed Render backend URL, no trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:4000
